import React from "react";
import { DEFAULT_HOURS_INTERVAL } from "../constants";
export declare const CurrentTime: React.FC<{
    hoursInterval: typeof DEFAULT_HOURS_INTERVAL;
    rowHeight: number;
}>;
